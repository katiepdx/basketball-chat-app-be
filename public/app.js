// const button = document.getElementById('button');
// // io coming in through the cdn in script tag
// const socket = io();

// button.addEventListener('click', () => {
//   socket.emit('user-click', Date.now());
// });

// socket.on('increment-click', (counter) => {
//   button.textContent = counter;
// });
